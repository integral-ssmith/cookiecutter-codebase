{
  "name": "{{ cookiecutter.project_slug }}",
  {% if cookiecutter.private -%}"private": true,
  {%- else -%}"private": false,{%- endif %}
  "release": {
    "branches": ["{{ cookiecutter.default_branch }}"]
  }
}